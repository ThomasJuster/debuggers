{
  "name": "php-dap",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build:ts": "tsc --outDir out",
    "build:php": "npm run build:ts && docker build -f ./PHP_Dockerfile `# build-folder` . -t php-debugger",
    "build:lldb": "npm run build:ts && docker build -f ./LLDB_Dockerfile `# build-folder` . -t lldb-debugger",
    "build:python": "npm run build:ts && docker build -f ./Python_Dockerfile `# build-folder` . -t python-debugger",
    "demo": "node call-script-demo.mjs"
  },
  "author": "",
  "license": "ISC",
  "// type": "module",
  "dependencies": {
    "node-debugprotocol-client": "^0.5.0"
  },
  "devDependencies": {
    "ts-node": "^10.4.0",
    "typescript": "^4.5.4",
    "vscode-debugprotocol": "^1.50.1"
  }
}
